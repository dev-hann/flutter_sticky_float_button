name: sticky_float_button
description: A Flutter implementation of sticky float button.
version: 0.0.1
homepage: https://github.com/yoehwan/flutter_sticky_float_button

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
